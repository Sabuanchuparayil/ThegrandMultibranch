{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python3 -m venv .venv && . .venv/bin/activate && pip install --upgrade pip setuptools wheel && pip install git+https://github.com/saleor/saleor.git && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "bash -c 'source .venv/bin/activate && gunicorn grandgold_wsgi:application --bind 0.0.0.0:$PORT --workers 4 --timeout 120'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

