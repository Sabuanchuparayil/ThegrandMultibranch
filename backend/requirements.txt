# Saleor dependencies (Saleor itself is installed from GitHub during build)
# IMPORTANT: Saleor must be installed FIRST (before this file)
# Saleor will install compatible versions of graphene, graphql-relay, etc.
# This file only contains additional dependencies not provided by Saleor
# Note: Latest Saleor requires Django 5.0+ (uses db_default feature)
django>=5.0.0,<6.0
psycopg2-binary>=2.9.0
redis>=5.0.0
celery>=5.3.0
django-cors-headers>=4.2.0
django-filter>=23.2
gunicorn>=21.2.0
whitenoise>=6.5.0
boto3>=1.28.0
requests>=2.31.0
python-dotenv>=1.0.0
# Note: graphene, graphene-django, graphql-core, graphql-relay are installed by Saleor
# Do not add them here to avoid version conflicts
reportlab>=4.0.0
weasyprint>=60.0.0
dj-database-url>=2.1.0

