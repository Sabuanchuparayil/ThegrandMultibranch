# Branches Module

GraphQL API structure is defined in `schema.py`. To activate:

1. Install graphene-django: `pip install graphene-django`
2. Uncomment the code in `schema.py`
3. Register with Saleor's GraphQL schema

See Saleor documentation for extending GraphQL schema.

