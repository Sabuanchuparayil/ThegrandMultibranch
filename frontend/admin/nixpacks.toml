[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
# Use npm install to ensure all dependencies including TypeScript are installed
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

